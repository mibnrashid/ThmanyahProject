PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=thmanyah

# Set to true only in local dev — use migrations in production.
DB_SYNCHRONIZE=false
DB_LOGGING=false
